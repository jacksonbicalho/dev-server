name: Test
on:
  pull_request:
    branches: [master]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          registry-url: 'https://registry.yarnpkg.com'
      - run: yarn install --frozen-lockfile
      - run: yarn build
      - run: yarn test:cov
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GH_TOKEN.YARN_TOKEN }}
